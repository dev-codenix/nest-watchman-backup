import { HttpException } from '@nestjs/common';
import { IException } from '../interfaces';
import { Request } from 'express';

// export interface IStrategy {
//   execute(statusCode: number, exception: IException, request: Request): void;
// }
